(function (window) {
  'use strict';

  /**
   * Creates a new Model instance.
   *
   * @constructor
   */
  function Model() {

  }

  // Export to window
  window.app = window.app || {};
  window.app.Model = Model;
})(window);